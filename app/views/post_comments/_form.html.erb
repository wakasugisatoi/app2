<%= form_with model: [post_image, post_comment], local: false do |f| %>
  <%= f.text_area :comment, cols: "40", placeholder: "コメントをここに", id: "comment_textarea" %><br>
  <%= f.submit "送信する", class: "btn btn-primary mt-2" %>
<% end %>